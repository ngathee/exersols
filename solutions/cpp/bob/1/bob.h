#pragma once
#include <string> 
using namespace std; 

namespace bob {
    bool isQuestion(const string&);
    bool isAllCaps(const string&);
    bool isSilence(const string&);
    string hey(const string); 
}  // namespace bob
